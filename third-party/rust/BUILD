# This BUILD file exists so that Bazel can address Cargo.toml and Cargo.lock
# as labels (//third-party/rust:Cargo.toml, //third-party/rust:Cargo.lock).
# No build targets are defined here — all crate targets live in @crates//.

exports_files(["Cargo.toml", "Cargo.lock"])
